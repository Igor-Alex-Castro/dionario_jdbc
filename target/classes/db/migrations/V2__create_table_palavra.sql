CREATE TABLE PALAVRA (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOME VARCHAR2(255) NOT NULL,
    SIGNIFICADO VARCHAR2(1000),
    DICIONARIO_ID NUMBER NOT NULL,
    CONSTRAINT FK_PALAVRA_DICIONARIO FOREIGN KEY (DICIONARIO_ID)
        REFERENCES DICIONARIO(ID)
);