CREATE TABLE origem (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tipo VARCHAR2(50),
    titulo VARCHAR2(150),
    pag NUMBER,
    palavra_id NUMBER UNIQUE, -- garante 1-para-1
    CONSTRAINT fk_origem_palavra
        FOREIGN KEY (palavra_id)
        REFERENCES palavra(id)
);
